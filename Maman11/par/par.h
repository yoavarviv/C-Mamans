#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#define MAX_SIZE 100

/* 
    This function recieves text from the user, and checks it with various
    checks to check whether the text is balanced or not.
    @returns True if the text is balanced, False otherwise.
*/
bool isFileBalanced();
